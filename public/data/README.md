# Local route data

`routes.json` is generated locally by `npm run import-health -- /path/to/apple_health_export`.

It contains precise locations and timestamps, so it is intentionally excluded from Git.
